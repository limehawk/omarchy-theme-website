{
  "name": "omarchy-theme-website",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build.js",
    "dev": "node scripts/build.js && bunx serve out -p 3000",
    "serve": "bunx serve out -p 3000",
    "deploy": "npm run build && npx wrangler pages deploy out/ --project-name=omarchy-theme-website"
  },
  "devDependencies": {
    "@tailwindcss/cli": "^4",
    "marked": "^15.0.0",
    "sanitize-html": "^2.13.0",
    "smol-toml": "^1.6.1",
    "tailwindcss": "^4",
    "wrangler": "^4.71.0"
  }
}
